server:
  port: 8080
spring:
  cloud:
    gateway:
      routes:
        - id: auth-pass-through
          uri: http://localhost:8081
          predicates:
            - Path=/health/**, /auth/**
management:
  endpoints:
    web:
      exposure:
        include: "*"